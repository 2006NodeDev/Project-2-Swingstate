version: 2.1
orbs:
  slack: circleci/slack@3.4.2
jobs:
  build-swingstate:
    docker:
    - image: circleci/node:12.18.2
    -working_directory: ~/Project/Project-2-Swingstate/*
    steps:
      - checkout:
          path: ~/Project-2-Swingstate
      - run: 
          name: Install
          command: |
            npm install
      - run: 
          name: Test
          command: |
            npm test
      - run: 
          name: Build
          command: |
            npm run build
      - slack/status:
          channel: G017L6Z17NZ
          mentions: 'CHANNEL'
          success_message: 'Code is ready for you to pull from the testing branch'
          failure_message: 'You should probably tell Tanique that the code failed'
workflows:
    build-and-test:
      jobs:
        - build-swingstate